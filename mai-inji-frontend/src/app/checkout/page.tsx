import { CheckoutForm } from '@/components/CheckoutForm';

export const metadata = {
  title: 'Checkout - Mai Inji',
  description: 'Complete your order',
};

export default function CheckoutPage() {
  return <CheckoutForm />;
}
